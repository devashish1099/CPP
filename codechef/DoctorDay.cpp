#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t,N;
	ll x;
	cin >> t;
	while(t--){
		cin >> N >> x;
		vector<int>population;
		for(int i = 0 ; i < N ; i++){
			int a;
			cin >> a;
			population.push_back(a); 
		}

	}
	return 0;
}